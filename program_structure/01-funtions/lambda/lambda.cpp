#include <iostream>

//	A lambda expression in C++ is a shorthand syntax for defining an anonymous function object
//	that can be used in place of a function pointer. It allows you to define a function on the fly
//	and pass it as a parameter to another function or use it directly.
//
//	A simple syntax of lambda expresion is:
//	[capture-list](parameter-list) -> return-type { function-body }


int main() {


	return 0:
}
